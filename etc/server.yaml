# Server Configuration
server: localhost
port: 10101

# Settings
settings:
    debug: 1
    protocol: json
    database:
        type: mysql
        name: nsmf
        user: nsmf
        pass: passw0rd.
        host: localhost
        port: 3306

# Enable Modules
modules:
    - cxtracker
    - snort
    - daemonlogger
